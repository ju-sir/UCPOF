# Qwen 7B 模型配置

# 模型路径
model_path: "/data/models/Qwen2.5-7B-Instruct"

# 模型参数
params:
  torch_dtype: "bfloat16"
  device_map: "auto"

# 生成参数
generation:
  max_new_tokens: 20
  pad_token_id: null  # 会在代码中自动设置
  eos_token_id: null  # 会在代码中自动设置
  do_sample: false
